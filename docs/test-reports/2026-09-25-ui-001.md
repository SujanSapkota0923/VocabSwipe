# Test report — UI-001 mobile-first UI/UX

Date: 2026-09-25 · Commit under test: `b2e7426` (re-run on the working tree
of the workflow setup, same templates/CSS/JS).

## Executed

| Check | How | Result |
| --- | --- | --- |
| Django suite | `DJANGO_DEBUG=1 python manage.py test` | 48 passed, 0 failed |
| Migrations | `makemigrations --check --dry-run` | No changes |
| Production static build | `collectstatic` with `DJANGO_DEBUG=0` | Succeeded |
| Viewport and flow check | `tests/ui/viewport_check.js` on a seeded throwaway DB, headless Chromium, 320×640, 375×667, 390×844, 430×932, 768×1024, 1280×800 | `NO PROBLEMS` |
| Screenshots | Reviewed by eye at every size: game, drag, revealed, timer, finish, dashboard, menu, home, explore, login, signup | Fixed during development: big gap between card and buttons on tall screens, over-wide dashboard summary on desktop, heavy timer toggle |
| Live site | `curl` of `/explore/` on the custom domain and the `onrender.com` host | 200 on both, new tab bar markup present |

What the viewport check asserts at each size: no horizontal overflow on home,
explore, login, signup and dashboard; game page does not scroll; controls
inside the viewport; drag right shows the stamp and releases into a revealed
card with a status POST; Next button advances; ← then Space advances;
buttons answer; a double tap on Next does not skip; known/review counts are
2/1; timer mode renders; the 10-card deck reaches the finish screen; the list
menu closes on an outside tap; no console errors.

## Acceptance criteria

See `tasks/active.md` → UI-001. All verified except the two below.

## Not verified

- iOS Safari and Android Chrome on real hardware: safe-area insets, the collapsing address bar with `100dvh`, rubber-band scrolling, real touch swipe feel. Headless Chromium drives pointer events with a mouse and does not emulate these.
- Screen-reader output (VoiceOver/TalkBack). Labels and live regions are in the markup but were not listened to.

## Regressions

None found.

## Recommended

Owner checks the live site on one iPhone and one Android phone before
closing UI-001.
